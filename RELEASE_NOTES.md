# Icefox 3.1.2

Icefox 3.1.2 优化朋友圈相册详情和评论交互，修复友情链接页面在不同永久链接规则下的自动发现，并继续将主题、Icefox 伴生插件和 IcefoxStorage 对象存储插件统一为同一版本号。

## 更新内容

- 友情链接页面地址留空时，自动查找已发布的 `links-page.php` 页面，并通过 Typecho 路由生成实际永久链接。
- 朋友圈相册详情头部随机使用相册照片作为背景，增加深色遮罩并保持标题、返回入口和顶部控件清晰可见。
- 信息流和文章详情的点赞/评论操作区采用更紧凑的布局与文案，并改善点赞列表和评论列表的视觉分隔。
- 长评论超过四行时显示“展开”，支持完整阅读后收起；初始内容、无限滚动内容和新提交评论使用同一逻辑。
- 为本次变更的主题样式和脚本增加 `3.1.2` 缓存键，直接覆盖文件后即可加载新资源。

## 发行文件

- `icefox-3.1.2.zip`：主题，解压到 `usr/themes/icefox/`。
- `Icefox-Plugin-3.1.2.zip`：必需的伴生插件，解压到 `usr/plugins/Icefox/`。
- `IcefoxStorage-3.1.2.zip`：使用 R2/S3 时安装，解压到 `usr/plugins/IcefoxStorage/`。
- `SHA256SUMS`：三个 ZIP 文件的 SHA-256 校验和。

## 升级说明

1. 升级前备份 Typecho 数据库、主题配置和本地上传目录；使用 R2/S3 时同时备份存储桶。
2. 直接覆盖 `usr/themes/icefox/`、`usr/plugins/Icefox/` 和已安装的 `usr/plugins/IcefoxStorage/` 文件，保留现有配置与数据。
3. 本版本不要求停用、卸载或重新安装插件，也不需要手动执行数据库迁移。
4. 使用对象存储时继续参考主题包 `deploy/php-uploads.ini` 配置 PHP 上传限制。
5. 主题样式和脚本带有新的缓存键；如站点前方配置了忽略查询参数的 CDN，请刷新对应静态资源缓存。

## 兼容性

- Typecho 1.2.0 或更高版本
- PHP 7.0 或更高版本
- MySQL 或 MariaDB
